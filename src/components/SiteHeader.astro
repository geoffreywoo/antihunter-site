---
const {
	active = '',
	showStatus = false,
	subtitle = '',
} = Astro.props as {
	active?: 'home' | 'roadmap' | 'specs' | 'treasury' | 'dev' | '';
	showStatus?: boolean;
	subtitle?: string;
};

const nav = [
	{ label: 'prophecy', href: '/', key: 'roadmap' },
	{ label: 'canon', href: '/canon', key: 'specs' },
	{ label: 'acts', href: '/acts', key: 'dev' },
	{ label: 'pilgrimage', href: '/pilgrimage', key: 'treasury' },
	{ label: 'sanctum', href: '/sanctum', key: 'tokenomics' },
	{ label: 'traces', href: '/treasury', key: 'telegram' },
] as const;

function pillClass(_key: string) {
	// legacy tailwind pill helper (kept to avoid churn; unused in ascii-whitepaper mode)
	return '';
}
---

<div class="site-header">
	<div class="brand">
		<a href="/" class="brand">
			<img src="/cryptopunk5730_openclaw_lobster.png" alt="anti hunter pfp" loading="eager" />
			<div>
				<div class="brand-title">anti hunter</div>
				{subtitle ? <div class="tiny muted">{subtitle}</div> : <div class="tiny muted">hypercapitalist intelligence engine</div>}
			</div>
		</a>
	</div>
	<nav class="nav tiny">
		{nav.map((n) => (
			<a href={n.href}>[{n.label}]</a>
		))}
		<a href="https://x.com/AntiHunter59823" target="_blank" rel="noreferrer">[x]</a>
	</nav>
</div>
<hr class="rule" />
